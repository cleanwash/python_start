# 세트는 {} 순서 x, 중복 불가 
# 리스트 [] 순서 o, 중복 가능

# a= {} 라고 쓰면 딕셔너리가 된다 
# 반드시 a = set()라고 써야된다. 

'''
remove('값)=> 없으면 에러 발생(엄격)
discard('값')=> 없으면 그냥 넘어감(유연)

set의 가장 큰 장점은 -> 중복 제거 -> List->Set->List를 통해서 처리하는 경우가 많음

교집합, 합집합, 차집합을 지원한다 -> 집합 연산을 쉽게 할 수 있다. 
'''

