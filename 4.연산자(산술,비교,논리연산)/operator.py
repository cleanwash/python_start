print(f'더하기: 1 + 1 = {1 + 1}')

#같다 ==, 
#같지 않다 !=,

#둘다 참이어야 한다 and 
#하나라도 참이면 된다 or

#not:결과를 반대로 뒤집음 

# 컴퓨터(Computer)의 뜻은 원래 '계산하는 사람'입니다.


# 산술 연산
# 기본 사칙 연산: 더하기(+), 빼기(-), 곱하기(*), 나누기(/)
# 나눗셈 3총사: 나누기(/), 몫(//), 나머지(%)
# 거듭제곱: **
# 비교 연산
# 크다(>), 작다(<), 크거나같다(>=), 작거나같다(<=)
# 같다(==), 같지않다(!=)
# 주의: a = 10은 대입(값을 넣음)이고, a == 10이 비교(같은지 질문)입니다!
# 논리 연산
# and: 둘 다 참이어야 True
# or: 둘 중 하나만 참이어도 Treu
# not: 결과를 반대로 뒤집음(참일 경우 False, 거짓일 경우 True)


