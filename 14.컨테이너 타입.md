List [], 순서 O, 변경 O 중복 O
Tuple () 순서 O, 변경 X, 중복 O
Dict {k:v} 순서X, 변경O, 중복 key(X)
Set {} 순서 X, 변경 O, 중복 X  -> 추가 삭제가 가능하다. 

순서가 중요하다 -> List, Tuple, Str
순서는 상관없다 -> Dict, Set 

변경가능 -> List, Dict, Set 
변경불가 -> Tuple, Str

대부분의 경우 : List(데이터 관리, 순서 필요)
이름표(검색)가 필요할 때:
Dict(회원정보, 설정값)
절대 변하면 안되는 것 
Tuple(좌표, 비밀번호, 상수 묶음)
중복을 없애고 싶으면 :Set

Set은 인덱싱으로 조회는 안되고, in으로 가능하다. 

